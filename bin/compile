#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Found mamute"

if [ ! -s $1/mamute ]; then
  echo "mamute was empty" | indent
  exit 1
fi

set -e

# Parse args
BUILD_DIR=$1
CACHE_DIR=$2

BIN_DIR=$(cd $(dirname $0); pwd) # absolute path

source $BIN_DIR/install_jdk